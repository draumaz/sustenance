<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose drives all UI; this base theme just removes the action bar. -->
    <style name="Theme.Heartwood" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/splash_bg</item>
    </style>

    <style name="Theme.Heartwood.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Heartwood</item>
    </style>
</resources>
